export enum Theme {
    dark = 'dark',
    light = 'light',
}
export enum Priorities {
    default = 'Обычный',
    medium = 'Средний',
    important = 'Важный'
} 
export enum bTypes {
    dropDown = 'dropDown',
    to = 'to',
    listItem = 'listItem',
    border = 'border',
    create = 'create',
    exit = 'exit',
    select = 'select',

    withColor = 'withColor'
}
export enum Visibility {
    hidden = 'hidden',
    visible = 'visible',
}