export enum Theme {
    dark = 'dark',
    light = 'light',
}
export enum Priorities {
    default = 'Обычный',
    medium = 'Средний',
    important = 'Важный'
} 
export enum bTypes {
    dropDown = 'dropDown',
    to = 'to',
    listItem = 'listItem',
    border = 'border',
    exit = 'exit',
    select = 'select',
}
export enum Visibility {
    hidden = 'hidden',
    visible = 'visible',
}